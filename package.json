{
  "name": "opt-out-manager",
  "version": "0.1.0",
  "private": false,
  "description": "Opt-out manager - Cloudflare Worker with Google (coefficient.org) auth and S3 sync",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "aws4fetch": "^1.0.20",
    "hono": "^4.6.3",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260911.1",
    "typescript": "^5.6.2",
    "wrangler": "^4.65.0"
  }
}
